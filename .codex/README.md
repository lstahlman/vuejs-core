# Codex agent package (Vue core)

Codex-oriented agent definitions for this monorepo. Canonical project instructions live in [`../AGENTS.md`](../AGENTS.md). Portable skills live in [`../.agents/skills/`](../.agents/skills/). Cursor-only rules live in [`../.cursor/rules/`](../.cursor/rules/).

| Path | Purpose |
| --- | --- |
| `agents/` | Custom agents mirrored from `.cursor/agents/` |

Edit `.cursor/agents/` as the source of truth and update these mirrors in the same PR.
